import { ContainerpdiDirective } from './containerpdi.directive';

describe('ContainerpdiDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerpdiDirective();
    expect(directive).toBeTruthy();
  });
});
