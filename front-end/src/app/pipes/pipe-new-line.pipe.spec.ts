import { PipeNewLinePipe } from './pipe-new-line.pipe';

describe('PipeNewLinePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNewLinePipe();
    expect(pipe).toBeTruthy();
  });
});
